http:
  routers:
    focuss-uptime-kuma-http:
      rule: Host(`status.focussdev.space`)
      entryPoints:
        - http
      middlewares:
        - redirect-to-https
      service: focuss-uptime-kuma
    focuss-uptime-kuma-https:
      rule: Host(`status.focussdev.space`)
      entryPoints:
        - https
      middlewares:
        - focuss-uptime-kuma-frame-policy
      service: focuss-uptime-kuma
      tls:
        certResolver: letsencrypt
  middlewares:
    focuss-uptime-kuma-frame-policy:
      headers:
        customResponseHeaders:
          X-Frame-Options: ""
          Content-Security-Policy: "frame-ancestors 'self' https://app.focussdev.space https://*.focussdev.space https://focussdev-hub.pages.dev"
  services:
    focuss-uptime-kuma:
      loadBalancer:
        servers:
          - url: http://focuss-uptime-kuma:3001

